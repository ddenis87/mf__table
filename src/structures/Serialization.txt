Serialization
цикл по секциям settings (пропускаем те что вложенны)
  Получаем данные по секции из документа
  Если секция массив Цикл по данным
    вызываем SerializationArea(данные, имя секции)
  Если секция объект, вызываем SerializationArea(данные, имя секции)

SerializationArea(данные, имя секции)
Сохраняем имя данных в секцию
Если есть вложенные данные
Цикл по вложенным данным
  Плучаем вложенное имя секции
  Вызываем SerializationArea(вложенным данным, вложенное имя секции)